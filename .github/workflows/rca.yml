name: Sample RCA test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run tests and save logs
        id: run_tests
        continue-on-error: true
        run: |
          set -o pipefail
          npm test 2>&1 | tee logs.txt

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: logs.txt

      - name: Fail build on test failure
        if: ${{ steps.run_tests.conclusion == 'failure' }}
        run: exit 1

  rca:
    needs: build
    if: failure()
    uses: itsmeteja9/Agent-POC/.github/workflows/rca-reusable.yml@05b1872
    with:
      tool_repo: itsmeteja9/Agent-POC
      log_file: logs.txt
      log_artifact_name: build-logs
      caller_run_id: ${{ github.run_id }}
      artifact_name: rca-report
      python_version: '3.10'
